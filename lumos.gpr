with "aws";

project Lumos is

   for Source_Dirs use ("src");
   for Object_Dir use "obj";

   for Main use ("web_server.adb");

   package Compiler is
      for Switches ("Ada") use ("-gnatwa");
   end Compiler;

end Lumos;
